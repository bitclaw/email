{
  "name": "@bitclaw/email",
  "version": "1.0.0",
  "description": "Multi-provider email sending with React Email support",
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "bun test --ignore src/providers/smtp-errors.test.ts && bun test src/providers/smtp-errors.test.ts",
    "test:unit": "bun test --ignore src/providers/smtp.test.ts",
    "test:integration": "bun test src/providers/smtp.test.ts",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "biome check",
    "lint:fix": "biome check --write",
    "format": "biome format --write",
    "knip": "knip",
    "prepublishOnly": "npm run build && npm run test:unit",
    "publish:dev": "npm run build && npm publish --tag dev --access public",
    "publish:patch": "npm whoami && npm version patch && git push --follow-tags && npm publish --access public",
    "publish:minor": "npm whoami && npm version minor && git push --follow-tags && npm publish --access public",
    "publish:major": "npm whoami && npm version major && git push --follow-tags && npm publish --access public"
  },
  "keywords": [
    "email",
    "smtp",
    "resend",
    "nodemailer",
    "react-email",
    "multi-provider"
  ],
  "author": "bitclaw",
  "license": "MIT",
  "dependencies": {
    "@bitclaw/result": "github:bitclaw/result",
    "@react-email/render": "^1.0.0",
    "@types/nodemailer": "^8.0.0",
    "nodemailer": "^8.0.7"
  },
  "peerDependencies": {
    "react": ">=18.0.0",
    "resend": ">=4.0.0"
  },
  "peerDependenciesMeta": {
    "resend": {
      "optional": true
    }
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/bun": "^1.3.9",
    "@types/react": "^19.1.0",
    "knip": "^6.12.1",
    "react": "^19.1.0",
    "resend": "^6.9.2",
    "typescript": "^5.8.3"
  }
}
